angular.module('App_ESSALE_SocialNetworkTypes', []).controller('state1Controller', function ($scope, $location,$rootScope, $ocLazyLoad, $state) {
    //console.log('state1Controller');
   // $scope.txt = $state.current.data.pageTitle;
    $scope.submit = function () {
        alert('trongntt');
    }
});